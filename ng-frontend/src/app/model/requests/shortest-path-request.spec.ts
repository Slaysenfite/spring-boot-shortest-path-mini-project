import { ShortestPathRequest } from './shortest-path-request';

describe('ShortestPathRequest', () => {
  it('should create an instance', () => {
    expect(new ShortestPathRequest()).toBeTruthy();
  });
});
